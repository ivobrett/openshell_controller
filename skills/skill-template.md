---
name: Skill name
description: One-line description of what this skill sets up.
tags: example, template
agents: any
---

Replace this block with your prompt body. Everything below the second `---`
line is what gets copied into the agent when a user clicks Copy.

Use placeholders for any secrets or site-specific values, for example:

- API endpoint: `<YOUR_API_ENDPOINT>`
- API key: `<YOUR_API_KEY>`
- Bot token: `<BOT_TOKEN>`

## Step 1 — Set up the connection

Tell the agent to configure the integration:

<PLACEHOLDER_STEP_1>

## Step 2 — Verify

Ask the agent to verify the connection is working:

<PLACEHOLDER_STEP_2>
